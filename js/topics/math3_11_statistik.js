let data=[];const svgS=document.getElementById('svg-stat'),dL=document.getElementById('data-list'),meanE=document.getElementById('mean'),medE=document.getElementById('median');function addP(){let v=Math.floor(Math.random()*101);data.push(v);renderS();}function clearP(){data=[];renderS();}function renderS(){document.querySelectorAll('.dp').forEach(e=>e.remove());data.forEach(v=>{let c=document.createElementNS('http://www.w3.org/2000/svg','circle');c.setAttribute('cx',20+(v/100)*360);c.setAttribute('cy',100);c.setAttribute('r',5);c.setAttribute('fill','red');c.setAttribute('class','dp');svgS.appendChild(c);});if(data.length===0){dL.innerText='-';meanE.innerText='-';medE.innerText='-';return;}dL.innerText=data.join(', ');let sum=data.reduce((a,b)=>a+b,0);meanE.innerText=(sum/data.length).toFixed(2);let sorted=[...data].sort((a,b)=>a-b);let mid=Math.floor(sorted.length/2);medE.innerText=sorted.length%2!==0?sorted[mid]:((sorted[mid-1]+sorted[mid])/2).toFixed(2);}


function topicInit() {}
