const fs=require('fs'),path=require('path'),assert=require('node:assert/strict'),{JSDOM}=require('jsdom');const read=p=>fs.readFileSync(path.join(__dirname,'..',p),'utf8'),data=JSON.parse(read('lang/de.json')),id='bio_1_evolution';
(async()=>{const dom=new JSDOM(read('topics/template.html'),{url:'https://example.test/topics/template.html?topic='+id,runScripts:'outside-only'}),w=dom.window,d=w.document;await new Promise(r=>setImmediate(r));w.fetch=async()=>({ok:true,json:async()=>data});for(const f of ['curriculum','common','chapter-revisions','core-learning','renderer'])w.eval(read('js/'+f+'.js'));await w.renderTopic();let paths=0;const questions=w.currentChapterQuiz.questions;assert.ok(!questions.some(q=>q.id==='bio_1_evolution_extra_belege'));for(let i=1;i<=5;i++)assert.ok(questions.some(q=>q.id==='bio_evolution_s'+i));for(const [i,q]of questions.entries())if(/^bio_evolution_s[1-5]$/.test(q.id)){assert.equal(q.sectionIndex,Number(q.id.at(-1))-1);for(let choice=0;choice<q.answers.length;choice++){questions.forEach((item,j)=>d.querySelector('input[name="chapter_q_'+j+'"][value="'+(j===i?choice:item.answers.findIndex(a=>a.correct))+'"]').checked=true);w.submitChapterQuiz();const stored=JSON.parse(w.localStorage.getItem('sciverse_chapter_quiz_results'))[id];assert.equal(stored.lastPercent,Math.round(100*(questions.length-(q.answers[choice].correct?0:1))/questions.length));if(!q.answers[choice].correct)assert.deepEqual(stored.reviewQuestionIds,[q.id]);assert.ok(d.getElementById('chapter-quiz-result').textContent.includes(q.answers[choice].feedback));paths++;}}assert.equal(paths,15);assert.equal(w.currentChapterResult(id,{contentRevision:1,passed:true,bestPercent:100}).passed,false);dom.window.close();console.log('PASS: five revised evolution questions, all 15 answer paths, targeted review IDs, section mapping, practice exclusion and stale revision.');})().catch(e=>{console.error(e);process.exitCode=1;});
