const assert=require('node:assert/strict'),fs=require('fs'),path=require('path'),{JSDOM}=require('jsdom'),root=path.join(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8'),c=JSON.parse(read('lang/de.json')).klimawandel;const dom=new JSDOM(c.sections[2].content,{runScripts:'outside-only'}),w=dom.window,d=w.document;w.eval(read('js/topics/klimawandel.js'));w.enhanceClimateChangeAccessibility();for(const type of ['heat','drought','heavyRain','alps','missing','heat']){const actual=type==='missing'?'heat':type,button=d.querySelector(`[data-impact="${actual}"]`);button.focus();w.showClimateImpact(type);assert.equal(d.querySelectorAll('[aria-pressed="true"]').length,1);assert.equal(d.querySelector('[aria-pressed="true"]').dataset.impact,actual);assert.equal(d.activeElement,button);assert.equal(d.getElementById(button.getAttribute('aria-controls')).id,'impactText');assert.equal(d.getElementById('impactText').closest('svg'),null);assert.equal(d.getElementById('impactTitle').tagName,'H4');assert.ok(d.getElementById('impactText').textContent.length>100);}assert.equal(d.querySelector('foreignObject'),null);assert.equal(d.querySelector('svg').getAttribute('aria-hidden'),'true');assert.equal(d.getElementById('impactText').getAttribute('role'),'status');dom.window.close();console.log('PASS: four impact explanations in unconstrained HTML, selected-button semantics, valid control targets, focus preserved and unknown selection falls back consistently.');
