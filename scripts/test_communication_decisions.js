const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),{JSDOM}=require('jsdom');
const read=p=>fs.readFileSync(path.join(__dirname,'..',p),'utf8'),data=JSON.parse(read('lang/de.json')),id='dgb7_kommunikation',keys=require('./fixtures/dgb7_communication_keys.json');
const expected={cloud:[{key:1,reason:/Benannte Konten.*Lesefassung/},{key:2,reason:/Übertragung und Vergleich/}],account:[{key:0,reason:/Kategorien.*einzigartige Passwörter/},{key:2,reason:/unabhängigen Prüfweg/}],identity:[{key:1,reason:/Unterstützung.*nicht die Schuld/},{key:0,reason:/nachgeahmtes Profil.*übernommenes Konto/}]};
(async()=>{
 const dom=new JSDOM(read('topics/template.html'),{url:'https://example.test/topics/template.html?topic='+id,runScripts:'outside-only'}),w=dom.window,d=w.document;await new Promise(r=>setImmediate(r));w.fetch=async()=>({ok:true,json:async()=>data});for(const f of ['curriculum','chapter-revisions','common','core-learning','renderer'])w.eval(read('js/'+f+'.js'));await w.renderTopic();
 const before=JSON.stringify(w.localStorage);
 for(const [name,expectations]of Object.entries(expected)){
  const zone=d.querySelector('[data-communication-lab="'+name+'"]'),control=zone.querySelector('select'),cases=[...zone.querySelectorAll('[data-boundary-case]')],result=zone.querySelector('[role=status]');assert.equal(cases.length,2);assert.equal(d.getElementById(control.getAttribute('aria-describedby')),result);
  for(const [n,e]of expectations.entries()){control.value=String(n);control.dispatchEvent(new w.Event('change'));assert.equal(cases.filter(c=>!c.hidden).length,1);assert.equal(cases[n].hidden,false);assert.match(result.textContent,/Lies den Fall/);for(let a=0;a<3;a++){const b=cases[n].querySelectorAll('button')[a];b.click();assert.equal(result.textContent.startsWith('Richtig:'),a===e.key);if(a===e.key)assert.match(result.textContent,e.reason);assert.equal(b.getAttribute('aria-describedby'),result.id);}}
  zone.querySelector('[data-boundary-reset]').click();assert.equal(control.value,'0');assert.equal(d.activeElement,control);assert.match(result.textContent,/Lies den Fall/);
 }
 assert.equal(JSON.stringify(w.localStorage),before);const ids=[...d.querySelectorAll('[id]')].map(e=>e.id);assert.equal(new Set(ids).size,ids.length);assert.equal(d.querySelectorAll('[data-communication-tasks]>li').length,15);assert.equal(d.querySelectorAll('[data-communication-chain]>li').length,4);
 assert.equal(d.querySelectorAll('[data-communication-rights] dt').length,4);assert.equal(d.querySelectorAll('[data-communication-contributions]>li').length,3);
 assert.match(d.querySelector('[data-communication-solution="cloud"]').textContent,/HTTPS.*Server.*Ende-zu-Ende/s);
 assert.match(d.querySelector('[data-communication-tasks="project"]').textContent,/eigenem Konto.*keine Passwörter.*erneut.*erste.*Simulation/s);
 assert.match(d.querySelector('[data-communication-tasks="identity"]').textContent,/Beobachtung und Vermutung.*keine Übergriffe/s);
 assert.match(d.querySelector('[data-communication-solution="identity"]').textContent,/kein Beweis für ein gehacktes Konto/);
 assert.equal(w.chapterRevision(id),1);assert.equal(w.currentChapterResult(id,{contentRevision:0,lastPercent:100}).outdated,true);
 const qs=w.currentChapterQuiz.questions;assert.deepEqual(Array.from(qs,q=>q.id),Object.keys(keys));
 for(const [n,q]of qs.entries()){assert.equal(q.sectionIndex,Math.floor(n/2));assert.equal(q.answers.length,3);assert.equal(q.answers.findIndex(a=>a.correct),keys[q.id]);for(let a=0;a<3;a++){w.restartChapterQuiz();qs.forEach((item,j)=>d.querySelector(`input[name="chapter_q_${j}"][value="${j===n?a:keys[item.id]}"]`).checked=true);w.submitChapterQuiz();const result=JSON.parse(w.localStorage.getItem('sciverse_chapter_quiz_results'))[id];assert.equal(result.lastPercent,a===keys[q.id]?100:88);assert.deepEqual(result.reviewQuestionIds,a===keys[q.id]?[]:[q.id]);assert.ok(d.querySelector('#chapter-quiz-result').textContent.includes(q.answers[a].feedback));}}
 const practice=data[id].sections.flatMap(s=>s.quizzes).filter(q=>q.practiceOnly);assert.deepEqual(practice.map(q=>q.answers.findIndex(a=>a.correct)),[0,1,0]);assert.ok(practice.every(q=>q.answers.length===3&&q.answers.every(a=>a.pts===0)));dom.window.close();
 const paper=new JSDOM(read('topics/worksheet.html'),{url:'https://example.test/topics/worksheet.html?topic='+id,runScripts:'outside-only'}),pw=paper.window,pd=pw.document;pw.fetch=async()=>({ok:true,json:async()=>data});for(const f of ['curriculum','chapter-revisions','worksheet_generator','worksheet'])pw.eval(read('js/'+f+'.js'));await new Promise(r=>setImmediate(r));const material=pd.getElementById('ws-dgb-material');assert.equal(material.querySelectorAll('[data-communication-paper] .ai-paper-case').length,6);assert.equal(material.querySelectorAll('[data-communication-tasks]>li').length,15);assert.equal(material.querySelectorAll('[data-communication-solution],button,select,input').length,0);assert.equal(pd.querySelectorAll('#ws-solutions [data-communication-solution]').length,4);assert.equal(pd.getElementById('ws-solutions').hidden,true);assert.match(material.textContent,/keine Rechte zwischen Konten/);paper.window.close();
 console.log('PASS: 18 independently keyed cloud/account/identity decisions, 24 assessed answer paths, practice keys, revision/review IDs, reset/focus/storage, collaboration and privacy boundaries and all six paper cases with four separate solutions.');
})().catch(e=>{console.error(e);process.exitCode=1;});
