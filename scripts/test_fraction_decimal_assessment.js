const assert=require('node:assert/strict'),fs=require('fs'),path=require('path'),{JSDOM}=require('jsdom');const read=p=>fs.readFileSync(path.join(__dirname,'..',p),'utf8'),data=JSON.parse(read('lang/de.json'));
(async()=>{let paths=0;for(const id of ['math1_8_brueche','math1_9_dezimalzahlen']){const dom=new JSDOM(read('topics/template.html'),{url:'https://example.test/site/topics/template.html?topic='+id,runScripts:'outside-only'}),w=dom.window,d=w.document;await new Promise(r=>setImmediate(r));w.fetch=async()=>({ok:true,json:async()=>data});for(const f of ['curriculum','chapter-revisions','common','core-learning','renderer'])w.eval(read('js/'+f+'.js'));await w.renderTopic();assert.equal(w.currentChapterQuiz.questions.length,15);assert.equal(w.currentChapterResult(id,{contentRevision:2,passed:true,bestPercent:100}).passed,false);const expected=[...data[id].quizzes,...data[id].sections.flatMap(section=>(section.quizzes||[]).filter(q=>!q.practiceOnly))];assert.equal(expected.length,15);assert.deepEqual(Array.from(w.currentChapterQuiz.questions,q=>q.id).sort(),expected.map(q=>q.id).sort());for(const [index,q]of w.currentChapterQuiz.questions.entries())for(let choice=0;choice<q.answers.length;choice++){w.currentChapterQuiz.questions.forEach((item,i)=>d.querySelector('input[name="chapter_q_'+i+'"][value="'+(i===index?choice:item.answers.findIndex(a=>a.correct))+'"]').checked=true);w.submitChapterQuiz();const stored=JSON.parse(w.localStorage.getItem('sciverse_chapter_quiz_results'))[id];assert.equal(stored.lastPercent,q.answers[choice].correct?100:93);assert.ok(d.getElementById('chapter-quiz-result').textContent.includes(q.answers[choice].feedback));if(!q.answers[choice].correct)assert.deepEqual(stored.reviewQuestionIds,[q.id]);paths++;}dom.window.close();const sheet=new JSDOM(read('topics/worksheet.html'),{url:'https://example.test/site/topics/worksheet.html?topic='+id,runScripts:'outside-only'}),sw=sheet.window,sd=sw.document;sw.fetch=async()=>({ok:true,json:async()=>data});for(const f of ['curriculum','worksheet_generator','worksheet'])sw.eval(read('js/'+f+'.js'));await new Promise(r=>setImmediate(r));assert.equal(sd.querySelectorAll('#ws-content > .question-block').length,15);assert.equal(sd.getElementById('ws-solutions').hidden,true);assert.equal(sd.querySelectorAll('#ws-solutions .question-block').length,15);sheet.window.close();}assert.equal(paths,90);console.log('PASS: fractions and decimals each expose fifteen assessed questions; all 90 answer paths, precise review IDs, stale mastery and complete fifteen-question worksheets with hidden solutions.');})().catch(e=>{console.error(e);process.exitCode=1;});
